[project]
name = "backpacker-ai"
version = "0.1.0"
description = "RAG-based travel chatbot for solo backpackers and budget travelers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.0.0",
    "minsearch>=0.0.5",
    "openai>=1.50.0",
    "python-dotenv>=1.0.0",
]
